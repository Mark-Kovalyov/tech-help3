mod heaps {

    pub struct UnlimitedHeap<T : Ord> {
        size : i32;
        heap : Vec<T>;
    }

    impl UnlimitedHeap for Heap {
        fn size() -> i32 {
            return size;
        }
    }


}
