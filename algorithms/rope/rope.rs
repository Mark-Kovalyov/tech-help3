trait Rope {
    fn rebalance();
    fn index(i : i32) -> Char
    fn split(i : i32) -> (Rope, Rope)
    fn concat(that : Rope)
    fn insert(s : String, i : i32)
    fn append(s : String)
    fn delete(i : i32, j : i32)
    fn build() -> String
}
